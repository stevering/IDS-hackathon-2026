/**
 * Guardian System Prompt — re-exported from the MCP package (single source of truth).
 *
 * The webapp adds dynamic context (selected node, plugin context) in route.ts.
 * The core Guardian intelligence lives in @guardian/mcp-server/knowledge.
 */
export { GUARDIAN_SYSTEM_PROMPT } from "@guardian/mcp-server/knowledge"
