"use client";

// /chat — welcome screen / fresh-chat mode. Renders the same Home component
// as / and /chat/[id]; the URL ↔ state sync inside Home handles the rest.
export { default } from "../page";
